server {
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}

server {
    # Listen on port 443 for incoming SSL connections.
    listen 443 ssl;
    server_name _;

    # SSL Certificate Configuration
    ssl_certificate /etc/nginx/certs/cert.pem;
    ssl_certificate_key /etc/nginx/certs/key.pem;

    # Forward standard HTTP requests to the client app.
    location / {
        # Forward requests to the mcp-client service on its internal port 8000.
        # 'mcp-client' is the service name from docker-compose.yml.
